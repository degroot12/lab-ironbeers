
    <div class="img-beer">
        <img src="{{this.image_url}}">
    </div>
    <div class="beer-text">
        <h1>{{this.name}}</h1>
        <p class="tagline">{{this.tagline}}</p>
        <p>{{this.description}}</p>
    </div>
       
 